To run, open this folder in a command prompt. Then, type the following ...

$ python3 disasm.py -i [filename]

Replace 'filename' with the name of the file that contains the binary x64 machine code.
